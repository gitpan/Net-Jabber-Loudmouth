#use strict;
#use Net::Jabber qw(Server);

#my $pid = fork();
#die "can't fork" unless defined $pid;

#unless ($pid) {
#	my $server = Net::Jabber::Server->new();
#	$server->Start();
#}

#sleep 1;

1;
